<header role="header" class="bg-yellow bn ba-l bw4-l b--white">
  <section class="w-100 dt ma0 center">
    <center class="dtc v-mid tl ph4 pv5">
      <h1 class="cursor f1 lh-title mv1 mv3-ns tracked-tight">{{ collection.title }}</h1>
      {% if collection.description != blank %}
        <div class="rte b ma0 f3 f2-m f1-l lh-title tracked-tight">
          {{ collection.description }}
        </div>
      {% endif %}
    </center>
  </section>
</header>
