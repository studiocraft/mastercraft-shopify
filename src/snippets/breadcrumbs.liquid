{% unless template == 'index' or template == 'cart' or template == 'list-collections' %}
<nav class="breadcrumb" role="navigation" aria-label="breadcrumbs" class="wow animated fadeInDown">
  <ul class="list pa0 mv0">
    <li class="dib">
      <a class="link underline-hover dark-gray b ttu" href="/">{{ shop.name }}</a>
      <span aria-hidden="true">/</span>
    </li>
    {% if template contains 'page' %}
    <li class="dib">
      <span class="dark-gray b ttu">{{ page.title }}</span>
    </li>

    {% elsif template contains 'product' %}
    <li class="dib">
      {% if collection.url %}
        {{ collection.title | link_to: collection.url }}
      {% endif %}
      <span class="dark-gray b ttu">{{ product.title }}</span>
    </li>

    {% elsif template contains 'collection' and collection.handle %}
    <li class="dib">
    {% if current_tags %}
      {% capture url %}/collections/{{ collection.handle }}{% endcapture %}
      {{ collection.title | link_to: url }}
      <span class="dark-gray b ttu">{{ current_tags | join: " + " }}</span>
      {% else %}
      <span class="dark-gray b ttu">{{ collection.title }}</span>
    </li>

    {% endif %}
    {% elsif template == 'blog' %}
      <span aria-hidden="true">/</span>
      {% if current_tags %}
        {{ blog.title | link_to: blog.url }}
        <span aria-hidden="true">/</span>
        <span>{{ current_tags | join: " + " }}</span>
      {% else %}
      <span>{{ blog.title }}</span>
      {% endif %}
    {% elsif template == 'article' %}
      <span aria-hidden="true">/</span>
      {{ blog.title | link_to: blog.url }}
      <span aria-hidden="true">/</span>
      <span>{{ article.title }}</span>
    {% else %}
     <span aria-hidden="true">/</span>
     <span>{{ page_title }}</span>
    {% endif %}

  </ul>

</nav>
{% endunless %}
